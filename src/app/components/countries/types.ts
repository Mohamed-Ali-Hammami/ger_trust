export interface CountryProps {
  title: string;
  emoji: string;
  description: string;
  benefits: string[];
  cta: string;
  image: string;
}

export interface CountryData {
  title: string;
  emoji: string;
  description: string;
  benefits: string[];
  cta: string;
  image: string;
}
